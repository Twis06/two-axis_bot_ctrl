# Packet 2C: freeze the deterministic baseline

Scope and acceptance: [EXECUTION_PLAN.md](../../docs/process/EXECUTION_PLAN.md) §5, Packet 2C. All results are **Simulated** or **Calculated**.

**Gate result:** independent review PASS WITH ISSUES (no Critical findings, 2 Important). Both are resolved (§6), and the freeze was re-made after the one controller fix.

## 1. What is frozen

- **Baseline fingerprint `7d857df507c389c9`,** re-made after the review fix I-A (the first freeze was `99d13231c51cb77e`). This is the sha256 over the modules in `exp/evidence.py` `BASELINE_SOURCES`: every controller and simulator module the baseline imports. It excludes:
  - the Task 3 estimator `ctrl/payload_estimator.py` (another agent's uncommitted work);
  - the legacy comparator;
  - the scoring code `sim/metrics.py`, which each run manifest hashes separately.
- **Git state:** the evidence was regenerated at commit `9851222` with the baseline sources clean.
- **Configuration:** `BaselineController()` defaults (`yaw_info="estimate"`, governor and yaw monitor on, T_design 7 ms, α 16, ω_i 0.1·ω_c) with `DriveSupervisor()` defaults, and the `SimConfig()` defaults from `sim/params.py`. Each run's exact configuration, including the scenario-specific overrides, is in `report/task2_runs.json`.
- **Code changes made at the freeze** (small, requested by the 2B and 4A reviews):
  - the host notice-log cap is raised to 2000;
  - the dead stale-feedback branch is removed from the host;
  - the drive logs which limit is active (`i_lim_sched` from the configured schedule, `i_lim_safety` from the supervisor).

  The controller's behaviour is unchanged by these edits. The independent reviewer ran 9 recorded run specifications on b7665aa and on the freeze and got bit-identical log digests (q, i, q_ref, mode, q_c, suspended, ack).

## 2. Checks required by the plan

| Plan item | Result |
|---|---|
| Keep the 4.46 Hz design unless the implementation or information mode requires re-derivation | Kept. Neither the causal yaw estimate nor the anti-windup change touches the feedback loop. |
| Nominal PM ≥ 45° / GM ≥ 6 dB; combined corner PM ≥ 30° / GM ≥ 6 dB (Calculated) | 49.4° / 13.5 dB at the 7 ms design point; 32.1° / 6.1 dB at 11 ms, J −30%, Kt +15%. Tests `test_design_margins` and `test_combined_delay_and_plant_corner_has_reserved_margins`. |
| Linearization limitations | Declared in [task2.md](../task2.md) §3 and §7. Gravity stiffness varies with angle and payload: −0.34 N·m/rad with 1 kg lateral at the worst angle, where the loop needs at least 0.46× of its gain. The delay approximation is cross-checked against the sampled simulator (critical gain 5.31× simulated vs 5.34× calculated at 1.2 ms CAN), but it proves nothing about the saturating, switched system. |
| All simulator and controller tests | Committed suite: 167 tests, OK. The working tree adds the Task 3 co-worker's 12 uncommitted estimator tests, for 179, OK. |
| Matched nominal, delay, derating, loaded and voltage-limited cases | [task2_numbers.md](../task2_numbers.md) contains A–E × 5 seeds with the matched legacy comparator, finite motions M1–M3 × 5 seeds, 12 fault/limit cases, and 100 sampled plants: 177 runs, each with a manifest. [task2_robustness.md](../task2_robustness.md) contains delay, saturation, the parameter grid and quantization, with the fingerprint and git state but no per-run manifests. |
| Publish a fingerprint and the operating-envelope limitations | Fingerprint at the top of both generated files; limitations in [task2.md](../task2.md) §8. |
| Rewrite task2.md around accepted behaviour and frozen evidence | Done. Every number is from the two generated files or a named packet report; checked line by line against the regenerated outputs. |

## 3. Evidence provenance (new in 2C)

- `exp/evidence.py` `RunBook`:
  - describes each run with an `exp.manifest` manifest *before* it runs;
  - requires every imported project module to be in the declared source set;
  - scores the run with `sim.metrics.evaluate` (4A.3);
  - records the run specification and metrics under its run_id.
- `task2_numbers.md` tables cite a run_id, or a run-set id that is the hash of the sorted run_ids, which are listed per sample in `task2_results.json`. The robustness runs are not yet recorded this way (review I-B; 4B).
- Both scripts publish through `staged_publish`. Outputs appear only after the whole evaluation succeeds and a source-change check passes. The first 2C run exercised this: the check correctly counted the concurrently edited 4A files as a change.
- The robustness readings for saturation rows A and D, and the quantization jitter attribution, are now computed from the numbers. Two prose readings written before the run had gone stale:
  - reading D described a watchdog trip, but the frozen run has none;
  - reading A described a braking clip, but the row shows 0% at the limit.

## 4. Acceptance against the plan

- **No open critical governor or fault-handling findings.** 2A: review PASS. 2B: review PASS WITH ISSUES, with no Critical findings; all 5 Important findings fixed and verified with the reviewer's reproducers (2B §9).
- **All required regression tests pass:** 167 committed tests OK (179 with the co-worker's uncommitted estimator tests).
- **Difficult D/E trials track within declared goals or explicitly reshape or reject with the promised fault behaviour.**
  - Nominal D/E are *reshaped*, to 83% and 51% net progress. Their governed RMS is 4.1° and 4.25°, with a 9.5° peak and no events. They do not meet the 4A tracking rule (0/5), and that is reported.
  - In the D/E stress trials, 5/20 and 6/20 fault. Each faulting trial trips once or twice and is then caught and held with the request suspended. There is no lockout and no cycling in this set. A lockout is reachable when the coupling model is badly wrong, which is declared in task2.md §8.4.
  - The loaded finite move M2 trips once and never completes (0/5).
  - These are promised fault behaviours, not tracking successes.
- **Zero event counts alone are not used as success.** Every table carries net progress and the 4A tracked/completed verdicts.

## 5. Remaining limitations carried forward

See [task2.md](../task2.md) §8. The ones that shape later packets:

- **For Task 3:**
  - the unmodelled payload (D/E reshaping, M2 not completed, suspensions);
  - the estimate-mode cost near the budget edge (C);
  - the missing propagation of estimator uncertainty into the governor budget.
- **For 4B:**
  - A is scored as a periodic request (4A review N5), so outage-induced rejoin time counts against its progress;
  - the robustness document still says "code hash" where it means the baseline fingerprint (cosmetic; the value matches);
  - the robustness runs should get per-run manifests (review I-B);
  - `staged_publish` staging lives in the project root, and two concurrent publishes left an empty staging directory (removed by hand; review m8).
- **For the system integrator:** suspension ends only on `replan()`; the fault acknowledgement has no session nonce; there is no coordinated yaw stop.

## 6. Independent review and gate

**Review: PASS WITH ISSUES.** No Critical findings, 2 Important and 9 Minor. The 2C checklist passed, with issues on the fingerprint/envelope item and the rewrite item. The 2B round-1 delta: I1 and I2 fixed, but with the regression I-A below; I3 and I5 fixed; I4 partly carried.

| # | Finding | Resolution |
|---|---|---|
| I-A | After a tracking fault under large coupling, the axis could stay in passive fallback for the rest of the run, even after yaw stopped and after `replan()`. The catch's coupling bound (`gov.cpl_peak`) only rose while the drive was in fallback. This was a side effect of 2B I1/I2. | **Fixed in `9851222`.** The bound is now the largest predicted coupling over the last 1 s, and `cpl_peak` decays in fallback. Regression tests `TestReview2C` (host-only and closed loop) fail on the old code. The reviewer's 12-run reproducer now re-arms 0.3–0.5 s after yaw goes quiet, with 0% fallback after 4 s (was 100%). Evidence regenerated; only the 60 ms outage rows changed (governed RMS 0.57–0.59° → 0.62°; progress 91% → 92%). The reviewer's alternative (decay the peak) locked out in 2/6 runs, so the window was chosen instead. |
| I-B | Provenance overclaimed: robustness rows have no run manifests. | Sentences qualified in task2.md (status box, §9) and here (§2, §3). Manifesting the robustness runs moves to 4B. |
| m1 | task2.md §8.2 "parks about 44°" came from unlabelled pre-freeze 2B data. | Labelled as the 2B tracking-fault matrix; the frozen seed-7 row (incompatibility, −39.6°) added in §6.3. |
| m2 | 13.2° should be 13.1°. | Fixed. |
| m3 | "Identical to pre-freeze" was unsupported. | Replaced by the reviewer's bit-identical check on 9 runs (§1). |
| m4 | The test count included 12 uncommitted co-worker tests. | Counts given separately (§2). |
| m5 | The capacity catch can re-trip up to lockout. | Declared, with the reviewer's lockout probe (task2.md §8.4). |
| m6 | The yaw-monitor trigger was missing from the host-policy table; the 0.3 s timer rarely fires under periodic coupling. | Row added (task2.md §5). |
| m7 | The anti-windup figures were attributed to the wrong source. | Fixed. |
| m8 | Staging leftovers. | Removed; recorded as a 4B item (§5). |
| m9 | The "modelled torque" wording, and "same stressed plants". | Reworded (nominal model; E 7 → 6). |

**Executor's gate: PASS.** The Packet 2C criteria are met:

- no open Critical governor or fault-handling finding;
- all required tests pass;
- D/E are explicitly reshaped or suspended with the promised containment;
- no success is claimed from event counts.

The baseline at fingerprint `7d857df507c389c9` is the frozen comparator for Task 3 and the controller for Task 5. The I-A fix was verified with the reviewer's reproducers and the regenerated evidence, not with a further full review round. A delta check is recommended in the 4B review.
