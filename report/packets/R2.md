# R2 — Task 5 windows, units and prediction inference

**Status:** implemented and verified; independent review pending. Corrected output is staged in [`report/staged/R2/`](../staged/R2/). Publication is R4. `report/task5_registration.json` is unchanged; this was checked byte-for-byte against the committed file. This is a **retrospective correction** of the original analysis: the same frozen controller (fingerprint `7d857df507c389c9`), the 12 original conditions and seeds 21–23 are replayed. It is not a new prospective experiment.

## Reproduced defects

1. **Mixed windows.** `_row()` restricted current to [2, 8) s but copied governed and original RMS, clipping and path speed from `summarize()`, which covers 0–8 s.
   - Regression: a synthetic log with large startup error and a small in-window error. The old code reports 5.0° governed RMS where the window value is 0.1°.
2. **Unit error.** `predicted_coupling_peak_current_A` copied `c_tau_cpl`, which is the controller's coupling feed-forward *torque* in N·m.
   - Regression: the old field name must be absent, the torque must be reported in N·m, and the current must be that torque divided by nominal Kt.
3. **Inference.** The registered numbers are component-level ideal arithmetic. The old write-up treated the coupling-torque match as confirmation, and it read the ±0.15 A tolerance, which contains 0 A, as a meaningful test of the +0.1076 A prediction.

Red/green check: `tests/test_task5_prediction.py`, 4 tests. Against the committed pre-R2 tree the result is `FAILED (failures=3)`; against this tree it is `OK`. Full suite: `Ran 220 tests … OK`.

## Changes (`exp/task5_prediction.py`)

- **One window.** Every windowed quantity (current, governed and original RMS and peak, clipping, path speed) uses the registered [2, 8) s. Whole-run safety evidence (events, trips, clipping, peak current, whole-run governed RMS) is kept in a separate `whole_run` field. Definitions are in `METRIC_DEFS` in the output.
- **Units.** `controller_coupling_ff_peak_torque_Nm` (N·m) replaces the mislabelled field, with `controller_coupling_ff_peak_current_A` = torque / nominal Kt (the commanded FF current). `ideal_true_coupling_peak_current_A` = true coupling / true Kt (what an ideal controller would need). The two currents are kept distinct.
- **Measured residual.** `coupling_residual_peak_Nm` is the coupling torque the unchanged FF leaves for feedback, max|τ_couple(t + T_act) − (Kt_true/Kt_nom)·ĉ(t)|, reported for FF on only.
- **Prediction matrix.** Each registered prediction is set against the quantity actually measured, with its unit, window, tolerance basis and verdict (`prediction_matrix` in the JSON; table in the staged `task5.md`).

## Corrected results (Simulated, staged)

| Registered prediction | Predicted | Measured | Verdict |
|---|---:|---:|---|
| Peak coupling torque of the yaw trajectory | 0.135622 N·m | 0.135622 N·m (logged true coupling) | **Consistency check only.** The simulator uses the same equation. |
| Peak coupling current at nominal Kt | 0.9687 A | 1.1597 A: the controller's commanded coupling FF current, median | **Failed** (±0.15 A). The frozen baseline's causal yaw estimate over-predicts the peak coupling by about 20%. |
| Extra ideal coupling current at Kt × 0.90 | +0.1076 A | +0.1076 A ideal, arithmetic; the commanded FF current changes by +0.0000 A | **Not tested.** The component is not separable in total current, and ±0.15 A includes 0 A. |
| Coupling residual left by nominal FF | +0.0136 N·m increase | −0.0007 N·m median, paired range −0.0027 to +0.0003 | **Failed** (±0.01 N·m). The over-predicting estimate is partly cancelled by the weaker motor; the total residual peak is about 0.030 N·m from estimation error. |
| Yaw FF remains valuable (qualitative) | — | FF off worse by 3.51–3.89° | **Supported** (3/3 pairs). |
| A weaker motor leaves more error (qualitative) | — | +0.054° median, paired range −0.002 to +0.058° | **Failed** as a uniform claim (2/3 pairs). |

**What changes from the published Task 5:**

- **Windowed governed RMS is higher,** because the quiet 0–2 s yaw ramp no longer dilutes it. With the weak motor and FF on the median is 0.328° (was 0.289°), against 0.274° nominal (was 0.242°). With FF off it is 3.936° (was 3.768°).
- **Current peaks are unchanged,** as they were already windowed.
- **The old "+0.047° paired" figure mixed windows.** The corrected paired change is +0.054° median, and it is not positive in every pair.
- **The explanation needs revising.** The component-level story — a weaker motor leaves a predictable coupling residual — does not survive in the frozen estimate-mode baseline. The prediction assumed feed-forward equals the true coupling, while the causal estimate over-predicts it. What does survive is that yaw feed-forward matters.

## Remaining limitations

- **No closed-loop tracking prediction was registered,** so §8's "closed-loop prediction using the actual frozen controller" remains **open**. A new prospective prediction (for example of the coupling residual including estimator bias, or of governed RMS) must be registered before it is run. This packet does not create or backdate one.
- **Plan-mode behaviour untested here.** The ~20% over-prediction of peak coupling by the causal estimator is a finding worth carrying into Task 2's information-mode discussion. It is measured here only on Run B, and whether plan mode would match the registered numbers is untested.
- **Old results remain published.** `report/task5.md` and `report/task5_results.json` are unchanged until R4 publishes the staged files.

## Gate

Executor's gate: PASS.

- Every metric has one documented window and unit.
- The regressions discriminate the old bugs.
- The original conditions are preserved.
- Unsupported prediction claims stay visibly open or failed.
- No controller change was needed.

Independent review of R2 is pending.
