# R6 — Independent final review

**Reviewer:** a sub-agent that implemented none of the corrective packets. It worked read-only, with scratch scripts outside the repository.

**Scope:** R1 scoring, R2 units and windows, R3 gate and challenges, R4 provenance and reproduction, and the claims in the memo, qualification plan, task answers, READMEs and HTML source.

## Round 1 verdict: FAIL (3 Important, 0 Critical)

**What the reviewer independently verified:**

- **Tests:** 257 tests OK.
- **Task 5:** regenerated, and byte-identical to the published files.
- **Manifests:** 3 manifest → source → metric traces rebuilt with identical run_ids and metrics.
- **Clean snapshot:** matches the repository.
- **HTML:** build manifest current.
- **R1:** 1641 waypoint visits all inside their own windows; counterexamples behave correctly.
- **R2:** units, windows and verdict labels.
- **R3:** gate values and the exact candidate configuration; the "exactly 0.0%" breakdown (30 bit-identical, 13 never usable, 17 late, 20 improve, 0 worse, mean +9.524%).
- **Memo:** about 35 numbers traced; 4 text pages.
- **Qualification plan:** one page, covering all four required parts.

None of the findings changes a published metric or the non-adoption decision.

| # | Finding | Resolution |
|---|---|---|
| **I1** | The gate's "no new fault" criterion compared only watchdog trips, suspension, rejection and lockout. A candidate adding `overspeed`/`overtemp` passed, and row event lists were truncated (`[:6]`, `[:8]`). | `gate_evidence` records untruncated `event_counts`. `safety()` compares every event type per pair, including re-arms; missing counts → `incomplete`. Tests: new overspeed/overtemp fails; equal counts pass; missing counts are incomplete; counts are untruncated. |
| **I2** | The memo said all safety criteria pass across 96 challenge runs, without saying the correction was usable in only 23/48 candidate runs (0/6 in each 9 s yaw B/C set). The gate merged `@late` sets and hid this. | The gate now reports exercise per onset set, and the L4 numbers print it. The memo, Task 3 and the HTML state 23/48, and that none of the 9 s-onset yaw B/C runs had an active correction. Test: per-sub-challenge exercise. |
| **I3** | The memo presented both registrations as preceding their tests. The project's own audit says Task 5's registration and results were committed together; Task 3's plan file was committed in the same second as the adaptive results. | Memo §3 and §5, Task 3 and the HTML now state the available ordering evidence. Task 3: protocol cited in `9554de8` at 10:38, before the candidate ran; plan file first committed with the results. Task 5: no git ordering evidence. |
| m1 | Challenge numbers linked `../../task3_challenges_registration.json`; the brief PDF is gitignored, so links to it are dead in a checkout. | Links are computed relative to the output directory. The PDF links are replaced by plain text. |
| m2 | "No correction reached the scored samples" is wrong for 2 runs: they were usable in the last dwell, but at constant reference, where the bumpless transfer cancels the change. | Wording corrected in the memo, Task 3 and the HTML. |
| m3 | The governor-oracle median (+78.6%) silently dropped 15 never-started pairs. | L4 prints `n = 35; 15 pairs unscored` whenever pairs are dropped. |
| m4 | Memo reproduction wording too strong. | First scoped to one environment. After the cross-platform check the memo states exactly what was verified: exact on macOS arm64 from a fresh locked install; identical outcomes and values within 3e-11 on Linux x86-64. |
| m5 | Memo omitted the sampled-plant sweep. | Added: A/B/C 13, 20 and 15 of 20 tracked with no events; D/E with events in 5 and 6 of 20. |
| m6 | INF-P disposition and Run A caveat understated. | Memo states that the load is attempted, sags to about −58° and is held near −49°. The voltage and trajectory caveat is restored. |
| m7 | `task5.md` lacked the revised explanation and the smallest design change. | Added to the generator (`_markdown`). |
| m8 | R1 is stricter than the old scorer for late-reached intermediate targets; undisclosed. | Disclosed in Task 3; no verdict affected. |
| m9 | Leftover `.stage-*` directory. | Removed; it is gitignored. |

**After the fixes:**

- **Tests:** the suite has 262 tests, all OK.
- **Memo:** re-rendered; the body is still 4 pages.
- **Evidence:** the changed sources are in the run_id source set, so everything is republished and the clean-snapshot check is repeated (below).

## Republication and clean-snapshot check after R6

- **`72c6d56`:** evidence republished from `bd9d35e`. No existing value changed; the additions are the new gate evidence fields. A clean snapshot of `72c6d56` reproduced it: 0 value differences, 18/18 figures byte-identical.
- **Reproducibility work** (see [R4](R4.md), "Locked environment and cross-platform reproduction"):
  - locked `uv` environment;
  - parallel Task 2 evaluation;
  - HTML builder independent of the gitignored brief;
  - cross-platform tolerance in one test.
- **Canonical publication `25559e3`:**
  - exact on the Mac from a fresh `uv sync` snapshot;
  - identical outcomes on Linux x86-64, with values within 3e-11.

## Status

- **Round 1:** FAIL. Its findings are resolved above.
- **Round 2 (release commit `961b6f2`): PASS WITH ISSUES.** A new independent reviewer found no Critical or Important findings, so the release gate is met. It reviewed a `git archive` copy of `961b6f2` only; uncommitted working-tree edits made by another agent during the review are **not** covered.

## Round 2: what was verified

- **I1–I3 and m1–m9 are resolved** in code, evidence and text.
  - Gate adversarial cases A1–A8 behave correctly.
  - All 453 rows carry `event_counts`.
  - Recomputing the gate from the published rows reproduces the published result.
- **Tests:** `uv run --frozen python -m unittest discover -s tests` ran 262 tests, OK; `uv lock --check` passes.
- **Parallel Task 2:** 177 unique run_ids; a wrong or unused run raises; the RNG order is preserved. `--jobs 1` gives 0 of 5,257 value differences and byte-identical figures.
- **Test tolerance:** `test_task4b` at 1e-12 still detects a 1e-10 perturbation and swapped draws.
- **HTML:** rebuilds byte-identical from the clean snapshot.
- **Cross-platform claims confirmed,** including the FMA cause (6,995 of 100,000 draws).
- **Release documents:** the memo is 4 body pages and the plan 1 page; about 40 numbers traced, all matching.

## Round 2 minor findings (open)

| # | Finding | Status |
|---|---|---|
| r2-1 | `challenges()` grid check does not flag *unregistered* challenge cells. Candidate-only rows under an unregistered label with a new fault give overall pass. No effect on the release: the published rows equal the registered keys. | **resolved:** the grid check flags unregistered challenge cells; regression test (A9 case) |
| r2-2 | `safety()` returns `incomplete` at the first pair missing counts, even if a later pair has a new fault. Conservative, since it never passes, but it misreports. | **resolved:** every pair is scanned, so a new fault gives fail even when another pair lacks counts; regression test |
| r2-3 | "Usable after onset in 23/48": in 2 of these runs the applied correction never exceeded 0.0004 N·m, and in 1 it was usable only 0.3% of the time. About 20/48 were materially exercised. | **resolved:** memo, Task 3 and HTML call 23/48 an upper bound on meaningful exercise and name the near-zero cases |
| r2-4 | `task3.md` and `task4.md` still quote 255 tests at `039f83b`, and task3 says "adoption gate (19)". | **resolved:** counts scoped by commit |
| r2-5 | Committed qualification plan: K<sub>e</sub> cannot be identified on a blocked axis, and "safe hold" contradicts the non-holding fallback. | **resolved** in the committed plan: blocked-axis K<sub>t</sub> identification, separate guarded low-speed K<sub>e</sub> step, and no promised position hold |
| r2-6 | R4's float-count method (5,412 of 44,893) is not stated. The reviewer counted 5,442 of 35,159 over a different leaf set; the worst difference, 3.1e-11, and 0 above 1e-9 match. | **resolved:** `tools/compare_evidence.py` committed; R4 states the method (5,412 of 45,717) |
| r2-7 | Commit-message wording "exactly the Monte Carlo runs" (every differing run_id is a Monte Carlo run, not every Monte Carlo run differs). | no fix needed; R4 text is correct |
| r2-8 | The memo's reproduction qualifier at line 10 should read "exact on macOS arm64; within 3e-11 on Linux x86-64". | **resolved** |
| r2-9 | "R6 pending" in `README.md` and `task4.md`. | open (update once accepted) |
